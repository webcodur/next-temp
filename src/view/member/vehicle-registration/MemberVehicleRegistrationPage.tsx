'use client';
import React from 'react';
import { usePageDescription } from '@/hooks/usePageDescription';

export default function MemberVehicleRegistrationPage() {
  usePageDescription('등록된 차량을 관리하고 신규 차량을 등록합니다.');

  return <div>차량등록 관리</div>;
} 