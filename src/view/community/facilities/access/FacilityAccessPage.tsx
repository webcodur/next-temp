'use client';
import React from 'react';
import { usePageDescription } from '@/hooks/usePageDescription';

export default function FacilityAccessPage() {
  usePageDescription('커뮤니티 시설 이용 현황을 관리합니다.');
  
  return (
    <div>시설 이용</div>
  );
} 